{
  "name": "remotebot",
  "version": "0.2.0",
  "private": true,
  "description": "Cầu nối Telegram cho AI coding agent.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "package:release": "node scripts/package-release.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
